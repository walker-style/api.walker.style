version: "3.8"
services:
  dev:
    build: .
    ports: 
      - 8080:8000
    volumes: 
      - ./src:/app/src
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
